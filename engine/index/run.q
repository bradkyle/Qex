
// Derives the next index tick from historic ticks from store
// inserts this back into store and sends events/updates all other engine 
// values

// TODO maintain smooth index event when stock split, etc.